export enum RoomWarningType {
  JOIN = 'joined',
  BAN = 'banned',
  KICK = 'kicked',
  PROMOTED = 'promoted',
  DEMOTED = 'demoted',
  MUTE = 'muted',
  LEAVE = 'left',
  OWNER_LEFT= 'owner left',
}
